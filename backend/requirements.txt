# Actarium Backend Dependencies
# Actual resolved versions from uv (Python 3.11)

# ── Web Framework ───────────────────────────────────────────────
fastapi>=0.136.3
uvicorn[standard]>=0.30.1
python-multipart>=0.0.9

# ── Config & Validation ─────────────────────────────────────────
pydantic>=2.7.3
pydantic-settings>=2.3.1

# ── LangChain Core ──────────────────────────────────────────────
langchain>=1.3.9
langchain-core>=0.3.0
langchain-community>=0.3.0     # For PyPDFLoader, BSHTMLLoader
langchain-google-genai>=4.2.5  # ChatGoogleGenerativeAI, GoogleGenerativeAIEmbeddings
langchain-chroma>=1.1.0        # Chroma vector store integration
langchain-text-splitters>=1.1.2

# ── LangGraph ───────────────────────────────────────────────────
langgraph>=1.2.5

# ── Google AI (used by langchain-google-genai) ──────────────────
google-generativeai>=0.7.2

# ── Google Cloud TTS (optional — gTTS fallback if absent) ────────
google-cloud-texttospeech>=2.16.3

# ── gTTS Fallback (no API key needed) ───────────────────────────
gTTS>=2.5.1

# ── Vector DB ───────────────────────────────────────────────────
chromadb>=0.5.3

# ── Document Loaders (used by langchain-community) ──────────────
pypdf>=4.2.0
beautifulsoup4>=4.12.3
requests>=2.32.3
lxml>=5.2.2

# ── Supabase ────────────────────────────────────────────────────
supabase>=2.31.0

# ── Rate Limiting ────────────────────────────────────────────────
slowapi>=0.1.9

# ── Utilities ───────────────────────────────────────────────────
python-dotenv>=1.0.1
